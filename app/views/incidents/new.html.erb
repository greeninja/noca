<%# link_to("<< Back to List", {:action => 'index'} ) %>

<h2>New Incident</h2>
  <%= form_for(@incident, :url => {:action => 'create'} ) do |f| %>
  <%= render(:partial => "form", :locals => {:f => f}) %><br />
  <%= submit_tag("Create Incident", class: "btn btn-success btn-default pull-right") %>
  <%= link_to("Cancel", {:action => 'index'}, class: "btn btn-danger btn-default pull-left") %><br />
  <% end %>
