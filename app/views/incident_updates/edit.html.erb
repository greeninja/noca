<%= link_to("<< Back", {:action => 'index'}) %>

<h2>Edit Update</h2>
<%= form_for(:update, :url => {:action => 'update', :id => @update.id, :incident_id => @incident.id}) do |f| %>
  <%= render(:partial => "form", :locals => {:f => f}) %>
  <%= submit_tag("Commit Update") %>
<% end %>

